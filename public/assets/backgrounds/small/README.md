# District Background Assets — small/ (unused)

This folder is no longer used.  All district backgrounds now live in the parent
folder (`public/assets/backgrounds/`) and are used for every context (preview,
print, and download).

See `public/assets/backgrounds/README.md` for the current conventions.
